using UnityEngine;

public class Player1_start : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        transform.position = new Vector3(-5, 5, (float)0.125);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
